function Footer( { area } )
{
    const areaStyle = {
        gridArea: "footer"
    };

    return (
        <footer style = { areaStyle } className = "">
        </footer>
    );
}

export default Footer;